<p id="notice"><%= notice %></p>

<h1><%= t('view.title_show') %></h1>

<div>
<p>
  <strong>タイトル:</strong>
  <%= @task.title %>
</p>

<p>
  <strong>詳細:</strong>
  <%= @task.content %>
</p>

<p>
  <strong>終了期限:</strong>
  <%= @task.expired_at %>
</p>

<%= link_to t('view.button_edit'), edit_task_path(@task) %> |
<%= link_to t('view.button_index'), tasks_path %>
